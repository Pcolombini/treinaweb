let tr = true;
let fs = false;

console.log(tr != fs ? 'Verdade' : 'Mentira');

/**
 * tr != fs ? 'Verdade' : 'Mentira'
 *  
 *   CONDIÇÃO   SE (TRUE)       SENÃO (FALSE)
 *  (tr != fs)  ?  ('Verdade')  :  ('Mentira')
 */

console.log((110 % 2 == 0) ? "Par" : "Impar");